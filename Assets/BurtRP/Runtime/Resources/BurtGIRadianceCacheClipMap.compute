#pragma kernel BurtGIRadianceCacheClipMapFallbackNoopCS

// Legacy aggregate XGI compute entry point.
//
// The radiance-cache clipmap, scene-voxel, and hash-grid kernels now live in
// split BurtGI*.compute assets. Keep this noop kernel so the old resource path
// can still import safely while runtime dispatches use the split shaders.
[numthreads(1, 1, 1)]
void BurtGIRadianceCacheClipMapFallbackNoopCS(uint3 dispatchThreadId : SV_DispatchThreadID)
{
}
